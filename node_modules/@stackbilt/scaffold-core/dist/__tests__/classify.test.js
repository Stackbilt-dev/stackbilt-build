"use strict";
/**
 * classify.test.ts — classification tests for @stackbilt/scaffold-core
 *
 * Adapted from stackbilt-web/src/lib/__tests__/scaffold-domain-fixtures.test.ts
 * Tests the classify module's pattern selection and quality profile inference.
 */
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const index_1 = require("../index");
// ─── Helpers ─────────────────────────────────────────────────────────────────
function fileContent(result, path) {
    const f = result.files.find((f) => f.path === path);
    return f?.content ?? '';
}
// Resolve the source_pattern string from classify result's traits
// (traits array may contain 'jwt-auth', 'hmac-stripe', etc.)
function sourcePattern(result) {
    const traits = result.classification.traits;
    // Identify source pattern from authentication trait
    if (traits.includes('hmac-stripe'))
        return 'stripe-webhook';
    if (traits.includes('hmac-sha256') || traits.some((t) => t.includes('webhook')))
        return 'generic-webhook';
    if (traits.includes('jwt-auth') && traits.includes('rest'))
        return 'workers-saas';
    if (traits.includes('streaming'))
        return 'ai-chat';
    if (traits.includes('ws-and-rest'))
        return 'durable-objects';
    if (traits.includes('scheduled-handler'))
        return 'cron-worker';
    if (traits.some((t) => t.includes('mcp') || t.includes('sse-jsonrpc')))
        return 'mcp-server';
    if (traits.includes('overlay-doc'))
        return 'hardening-overlay';
    return 'rest-api';
}
// ─── Tenancy guardrail tests (#177) ──────────────────────────────────────────
(0, vitest_1.describe)('scaffold domain fixtures — tenancy guardrail (#177)', () => {
    (0, vitest_1.it)('recognizes multi-tenant SaaS with org isolation as workers-saas pattern', () => {
        const result = (0, index_1.classify)('Multi-tenant SaaS API with organization-level data isolation');
        (0, vitest_1.expect)(result.pattern).toBe('workers-saas');
        (0, vitest_1.expect)(result.traits).toContain('jwt-auth');
    });
    (0, vitest_1.it)('does not misclassify workspace-scoped project management as cron or mcp', () => {
        const result = (0, index_1.classify)('Workspace-scoped project management with D1 and JWT auth');
        // Must not be scheduled (cron) or mcp-server
        (0, vitest_1.expect)(result.traits).not.toContain('scheduled-handler');
        (0, vitest_1.expect)(result.pattern).not.toBe('mcp-server');
    });
    (0, vitest_1.it)('recognizes tenant isolation with row-level security as workers-saas', () => {
        const result = (0, index_1.classify)('Tenant isolation API with row-level security in D1');
        (0, vitest_1.expect)(result.pattern).toBe('workers-saas');
        (0, vitest_1.expect)(result.traits).toContain('jwt-auth');
    });
});
// ─── Telephony/ads intent alignment tests (#176) ─────────────────────────────
(0, vitest_1.describe)('scaffold domain fixtures — telephony/ads intent alignment (#176)', () => {
    (0, vitest_1.it)('Twilio voice webhook produces wrangler.toml and D1 binding or schema tables', () => {
        const result = (0, index_1.buildScaffold)('Twilio voice webhook that transcribes calls and stores to D1');
        const wrangler = fileContent(result, 'wrangler.toml');
        (0, vitest_1.expect)(wrangler.length).toBeGreaterThan(0);
        // Should have either D1 binding or related schema
        const hasD1 = wrangler.includes('d1_databases') || wrangler.includes('D1');
        const hasSchema = result.files.some((f) => f.path.includes('schema'));
        (0, vitest_1.expect)(hasD1 || hasSchema).toBe(true);
    });
    (0, vitest_1.it)('SMS notification with Twilio + KV produces KV binding and is not a cron worker', () => {
        const result = (0, index_1.buildScaffold)('SMS notification service using Twilio and KV for dedup');
        const wrangler = fileContent(result, 'wrangler.toml');
        (0, vitest_1.expect)(wrangler.includes('KV') || wrangler.includes('CACHE')).toBe(true);
        (0, vitest_1.expect)(result.classification.traits).not.toContain('scheduled-handler');
    });
});
// ─── PII risk enrichment tests (#178) ────────────────────────────────────────
(0, vitest_1.describe)('scaffold domain fixtures — PII risk enrichment (#178)', () => {
    (0, vitest_1.it)('health records prompt produces PHI/HIPAA-specific threats', () => {
        const result = (0, index_1.buildScaffold)('Health records API storing patient prescriptions and diagnoses');
        const tm = result.governance.threatModel;
        (0, vitest_1.expect)(tm).toMatch(/PHI|HIPAA|patient/i);
        (0, vitest_1.expect)(tm).toMatch(/T-D1/);
    });
    (0, vitest_1.it)('payment card processing prompt produces PCI-specific threats', () => {
        const result = (0, index_1.buildScaffold)('Payment card processing with tokenization');
        const tm = result.governance.threatModel;
        (0, vitest_1.expect)(tm).toMatch(/PCI|PAN|cardholder|card data/i);
        (0, vitest_1.expect)(tm).toMatch(/T-D1/);
    });
    (0, vitest_1.it)('user profile storing PII produces PII-specific threats', () => {
        const result = (0, index_1.buildScaffold)('User profile service storing email, phone, and address data');
        const tm = result.governance.threatModel;
        (0, vitest_1.expect)(tm).toMatch(/T-D1/);
        (0, vitest_1.expect)(tm).toMatch(/personal|PII|retention|breach/i);
    });
    (0, vitest_1.it)('Twilio voice webhook produces telephony-specific threats', () => {
        const result = (0, index_1.buildScaffold)('Twilio voice webhook that transcribes calls and stores to D1');
        const tm = result.governance.threatModel;
        (0, vitest_1.expect)(tm).toMatch(/T-T1/);
        (0, vitest_1.expect)(tm).toMatch(/Twilio|recording|signature/i);
    });
});
// ─── Classification confidence and quality profile ───────────────────────────
(0, vitest_1.describe)('classification quality and confidence', () => {
    (0, vitest_1.it)('stripe webhook produces high confidence classification', () => {
        const result = (0, index_1.classify)('Build a Stripe webhook handler with HMAC verification');
        // Should score high (multiple keywords hit)
        (0, vitest_1.expect)(result.confidence).toBeGreaterThan(0.5);
        (0, vitest_1.expect)(result.traits).toContain('hmac-stripe');
    });
    (0, vitest_1.it)('generic fallback to api for vague intentions', () => {
        const result = (0, index_1.classify)('build something');
        // Should fall back to api pattern (rest-api source_pattern)
        (0, vitest_1.expect)(result.pattern).toBe('api');
    });
    (0, vitest_1.it)('compliance domains are detected for HIPAA content', () => {
        const result = (0, index_1.classify)('HIPAA-compliant patient health records API');
        (0, vitest_1.expect)(result.qualityProfile.complianceDomains).toContain('PHI');
    });
    (0, vitest_1.it)('compliance domains are detected for PCI content', () => {
        const result = (0, index_1.classify)('credit card payment processing API');
        (0, vitest_1.expect)(result.qualityProfile.complianceDomains).toContain('PCI');
    });
    (0, vitest_1.it)('enriched intention has injected stack/entity sections', () => {
        const result = (0, index_1.classify)('Podcast SaaS with D1, Stripe');
        (0, vitest_1.expect)(result.enrichedIntention.length).toBeGreaterThanOrEqual(result.traits.length > 0 ? 10 : 0);
    });
});
//# sourceMappingURL=classify.test.js.map