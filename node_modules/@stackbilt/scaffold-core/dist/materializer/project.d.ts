/**
 * materializer/project — project file generators
 *
 * Generates wrangler.toml, package.json, tsconfig.json, src/index.ts,
 * test/index.test.ts, README.md, schema.sql, and contract stubs.
 * Extracted from stackbilt-web/src/lib/scaffold-materializer.ts.
 * Pure — no CF Worker imports, no network calls.
 */
import type { ScaffoldFacts, ScaffoldFile } from '../types';
import { type Facts } from './adf';
interface DomainHint {
    id: string;
    match: (intention: string) => boolean;
    deps?: Record<string, string>;
    devDeps?: Record<string, string>;
    bindings?: string[];
    scripts?: Record<string, string>;
    envInterface?: string[];
    indexImports?: string[];
    indexBody?: string;
    extraFiles?: ScaffoldFile[];
}
export declare function detectDomainHints(intention: string): DomainHint[];
export interface PrdSections {
    stack: string[];
    entities: string[];
    integrations: string[];
}
export declare function parsePrdSections(intention: string): PrdSections;
export declare function buildPrdHint(prd: PrdSections, projectName: string): DomainHint | null;
export declare function renderSchemaSQL(entityNames: string[], projectName: string): string;
export declare function renderPackageJson(facts: Facts, projectName: string, hints?: DomainHint[], extraDeps?: Record<string, string>, extraDevDeps?: Record<string, string>): string;
export declare function renderWranglerToml(facts: Facts, projectName: string, hints?: DomainHint[]): string;
export declare function renderIndexTs(facts: Facts, hints?: DomainHint[]): string;
export declare function renderTestFile(facts: Facts, projectName: string): string;
export declare function renderReadme(facts: Facts, projectName: string, intention: string): string;
export declare function renderTsConfig(): string;
/**
 * Generate project-level ScaffoldFiles (package.json, wrangler.toml, tsconfig.json,
 * src/index.ts, test/index.test.ts, README.md, optional schema.sql + contract stubs)
 * for the given scaffold facts.
 *
 * @param scaffoldFacts - Structured scaffold facts from the classify pipeline
 * @param rawFacts      - Optional raw key-value facts from scaffold-cast (fallback to empty)
 */
export declare function generateProjectFiles(scaffoldFacts: ScaffoldFacts, rawFacts?: Facts): ScaffoldFile[];
export {};
//# sourceMappingURL=project.d.ts.map